import 'package:flutter/material.dart';

class PlayerColors {
  static const cyan = Color(0xFF00FFFF);       // Spiller 1 – Cyan (fra logo)
  static const magenta = Color(0xFFFF2DF7);    // Spiller 2 – Rosa (fra logo)
  static const green = Color(0xFF39FF14);      // Spiller 3 – Neon grønn ("GO")
  static const orange = Color(0xFFFFA500);     // Spiller 4 – Neon oransje
}
