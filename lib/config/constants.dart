
class GameConfig {
  static const double snakeSpeed = 100.0;
  static const int maxPlayers = 4;
  static const int pointsToWin = 5;
  static const Duration roundEndDelay = Duration(seconds: 2);
  static const double turnSpeed = 0.05;
}
